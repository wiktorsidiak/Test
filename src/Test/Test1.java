package Test;

import java.util.List;
import java.util.Locale;
import java.util.stream.Collectors;

/*
public class Test1 {

    public static void main(String[] args) {
        List<String> strings = List.of("gro u nd", "st y le", "soa p", "vi n e");
        List<String> resultList = removeWhiteSpaceAndUpperCaseFirstLetter(strings);
        System.out.println(resultList);
    }

    private static List<String> removeWhiteSpaceAndUpperCaseFirstLetter(List<String> strings) {
        return strings.stream().map(s -> s.)
}
*/